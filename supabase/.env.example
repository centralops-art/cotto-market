# Supabase Edge Function secrets. Set via `supabase secrets set --env-file supabase/.env`
# (local) or through the hosted project's dashboard -- never committed.
# Populated as later phases need them; not required for Phase 0.

RESEND_API_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
MAPBOX_TOKEN=
